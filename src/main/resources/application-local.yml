# Texto plano para desarrollar. Activar con --spring.profiles.active=local
logging:
  structured:
    format:
      console: ~
  pattern:
    console: "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] tenant_id=%X{tenant_id:-} request_id=%X{request_id:-} %logger{36} - %msg%n"