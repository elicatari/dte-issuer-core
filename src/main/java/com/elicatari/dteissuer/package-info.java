/**
 * Paquete base: {@code com.elicatari.dteissuer}.
 *
 * <p>Un solo módulo de aplicación. Hexagonal:
 * {@code domain} (Java puro), {@code application} (casos de uso y puertos),
 * {@code adapter} (HTTP, JPA, Rabbit) y {@code shared} (tenant, security, logs).
 */
package com.elicatari.dteissuer;